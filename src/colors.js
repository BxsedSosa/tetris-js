import r from "raylib";

export class Color {
  cellColors = () => {
    return [
      r.WHITE,
      r.GREEN,
      r.RED,
      r.ORANGE,
      r.YELLOW,
      r.PURPLE,
      r.BLUE,
      r.DARKBLUE,
    ];
  };
}
