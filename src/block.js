class Block {
  constructor() {
    let colors = new Color();
    this.id = id;
    this.cells = cells;
    this.cellSize;
    this.rotationState;
    this.colors = colors.cellColors();
  }

  block = () => {};
}
